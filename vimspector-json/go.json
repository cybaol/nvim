{
  "configurations": {
    "run": {
      "adapter": "delve",
      "filetypes": ["go"],
      "configuration": {
        "request": "launch",
        "mode": "debug",
        "program": "${fileDirname}/${fileBasename}"
      }
    }
  }
}
